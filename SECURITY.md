# Security Policy

Report vulnerabilities privately through GitHub's security advisory feature.
Do not open a public issue for credential exposure, arbitrary code execution,
unsafe model deserialization, or private dataset disclosure.

Only the latest tagged release is supported. Model files and `.oclp` projects
should be treated as untrusted input unless their provenance is known.

The frozen macOS dependency audit and its interpretation are published under
`docs/security/`.
